// cypress/support/index.js

// Aquí puedes incluir configuraciones globales para tus pruebas.
Cypress.Commands.add('login', (username, password) => {
    // Lógica para el inicio de sesión
});
